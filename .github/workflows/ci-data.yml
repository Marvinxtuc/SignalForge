name: Data Migration Gate

on:
  pull_request:
  push:
    branches:
      - feature/mvp-p0

permissions:
  contents: read

jobs:
  phase-aware-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Phase -1 placeholder
        run: |
          echo "Phase 0 placeholder only. Real migration checks will be enabled in Phase 1."
